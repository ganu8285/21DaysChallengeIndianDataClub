SELECT
	S.STAFF_ID,
	S.STAFF_NAME,
	S.ROLE,
	S.SERVICE,
	COUNT(SW.WEEK) AS weeks
FROM
	STAFF AS S
	LEFT JOIN staff_schedule  AS SW ON S.STAFF_ID = SW.STAFF_ID
GROUP BY
	S.STAFF_ID,
	S.STAFF_NAME,
	S.ROLE,
	S.SERVICE
ORDER BY
	weeks DESC;




